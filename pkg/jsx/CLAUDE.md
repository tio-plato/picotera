The document of the js scripting API is located at docs/scripting.md, you should keep the docs sync with the code.

If changing any interface, you should also check scripts in docs/example-scripts and migrate it to the new version.

If adding new features, you should make a new example script in docs/example-scripts.
