# Proposal

增加 unovis 图表库，然后在控制台加一个“概览”页面，加个可以切换 24h/1d/7d/1m 的选择框，再增加几个过滤框，可以按 api key/实际模型/上游模型/渠道过滤。然后下面用 bento box 显示一些统计数据，总 token 数，总请求数，总费用，总追踪数。再下面显示饼图，2列，左边显示token数分布图，右边显示费用分布图，维度同下文。再下面显示2列的面积堆叠图，图表可以自由按 apikey/实际模型/上游模型/渠道 维度聚合或是不聚合，图标按小时级显示每小时的总 token 数、费用、请求数、追踪数。
