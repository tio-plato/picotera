# 请求与追踪页面时间筛选器

我想给请求页面、追踪页面增加一个时间筛选器，可以选择开始时间和结束时间。请求页面就放在类型边上，追踪页面也参考请求页面加上。这里可能还需要一个时间选择组件，想办法设计一个，或者搞一个 headless 的第三方组件。

交互决定：时间选择组件做成始终内联展示（不要浮动浮层），label 在上、输入框在下，直接使用原生 `datetime-local` 输入框，无触发器、无应用按钮。
