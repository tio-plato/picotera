# 模型价格匹配

我在 `pkg/pricing/pricing.json` 放了一个模型价格表。我希望在控制面板上增加一个匹配模型价格的按钮，点击之后，能自动匹配到最合适的模型（id 编辑距离最短？），前端显示个对话框让用户选择一个点击确认，自动填充到前端。我希望匹配是 golang 后端计算，挑选几个合适的之后，再发到前端去选。发到前端去的格式应该和我们的模型价格格式一样，而不应该遵守 pricing.json 的格式。我们也只需要从 pricing.json 里读取一部分我们需要的字段就行了。
