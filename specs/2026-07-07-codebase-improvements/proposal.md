# 研究代码库中值得改进的地方

用户原始需求：研究一下我这个代码库有哪些地方值得改进的。

## 说明

这是一项研究型任务。目标是对 PicoTera 代码库进行系统性审查，找出值得改进的地方（代码质量、架构、正确性、性能、安全性、可维护性等方面），并将发现整理为可选的改进项，供用户挑选后进一步细化设计与执行计划。

研究范围覆盖后端（Go：`pkg/*`、`cmd/*`、`db/*`）与前端（`dashboard/`），但不包含第三方代码（`third_party/`、`.pnpm-store/`）。
