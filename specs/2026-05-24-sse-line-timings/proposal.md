# SSE 响应逐行时间记录

为 SSE 的响应记录每个事件到达的时间。在 artifacts 中，新增一个数组，每个元素都是一个时间差值，代表收到第n行响应的时候，离请求发起过去了多少时间。客户端渲染响应的时候，如果是 events 模式，那么给每个 event 加上收到的时间。如果是 raw 模式，增加一个收到时间的勾选功能，如果勾选，则将响应渲染成一个表格式的界面，左边是时间值，右边是每行内容。
