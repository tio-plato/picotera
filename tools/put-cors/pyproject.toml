[project]
name = "put-cors"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "oott123", email = "git@public.oott123.com" }
]
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.43.68",
]

[project.scripts]
put-cors = "put_cors:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
